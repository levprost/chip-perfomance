{% extends 'base.html.twig' %}

{% block title %}Structure{% endblock %}

{% block body %}
    <h1>Structure</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ structure.id }}</td>
            </tr>
            <tr>
                <th>Image_structure</th>
                <td><img src="{{ asset('uploads/images/' ~ structure.imageStructure) }}" width="100"></td>
            </tr>
            <tr>
                <th>Content_structure</th>
                <td>{{ structure.contentStructure }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('app_structure_index') }}">back to list</a>

    <a href="{{ path('app_structure_edit', {'id': structure.id}) }}">edit</a>

    {{ include('structure/_delete_form.html.twig') }}
{% endblock %}
